# x-engine

x-engine is a decision-support engine for time-constrained problem solving.

It provides task selection and time allocation recommendations in simulated exam-like environments.
The goal is to optimize strategic behavior under time pressure — not just correctness.

## Run locally

```bash
uvicorn app.main:app --reload
